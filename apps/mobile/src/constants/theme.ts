/**
 * Theme constants re-exported from the shared UI components package.
 * All mobile screens should import theme values from this file.
 */
export {
  colors,
  fontSize,
  fontWeight,
  lineHeight,
  fontFamily,
  enforceFontFloor,
  MIN_FONT_SIZE,
  MAX_FONT_SIZE,
  spacing,
  layout,
  shadows,
} from '@gentle-reminder/ui-components';
